# requirements-agent 项目约定

## 唯一工作台

- 日常工作台只运行本仓库根目录的 `app/` 与 `web/dist/`，唯一地址为 `http://127.0.0.1:8765/`，唯一活动数据目录为根目录 `data/`。
- 日常入口为根目录 `start-local.cmd`；脚本维护使用 `scripts/start.ps1` 和 `scripts/stop.ps1`。重复启动应复用同一实例，不自动换端口。
- 不从 `evidence/`、历史交付包、试跑源码副本启动第二个用户工作台。历史副本仅作归档，不能当作当前工程。
- 更新现有应用，不另建一套 UI 或另一个长期运行的预览实例。重启前检查活动任务并保护未保存输入；不能杀死未经核实的进程。

## 文件与验证

- 后端实现放 `app/`，前端放 `web/src/`，启动与维护工具放 `scripts/`，回归放 `tests/`，说明放 `docs/`。原始开发资料和历史报告保留原身份。
- 离线测试使用隔离数据和应用工厂；不让测试写入日常 `data/`。浏览器核对使用统一入口，不留下额外服务。
- 如测试确需独立临时服务，应在明确授权范围内限定生命周期，并在结束前关闭；不能再把临时端口交付为日常入口。
- 构建先输出到忽略的验证目录，检查后在停止服务期间更新 `web/dist/`；不要并行提供不同版本的工作台。
- `.env`、数据、模型调用证据和本机备份不得提交。保留用户已有修改及原始资料；2026-09-26 按用户授权将 `.env.example` 删除与合成 `原型/` 历史资料纳入 Git，不恢复或改写原件。
- 数据整理先作一致性备份；同 ID 的不同快照不静默覆盖，不为合并历史记录更改业务状态或重写试跑结论。

## Git 正式版本

- `main` 为正式源码分支，发布标签固定已验证的提交；当前版本与证据见 `docs/version-control-20260926.md`。`codex/` 分支用于开发或历史记录，不用分支名称代替版本验收。
- 提交应包含相互依赖的实现、测试和说明。发布前从干净检出核对必要文件与回归，不把含未跟踪源码的工作区测试归属于旧提交。
- `原型/` 与 `docs/requirements-agent-ui-handoff-v0.9/` 为历史设计参考，不作为当前业务规则或运行入口；展开目录已受控的重复 ZIP 保留本机并精确忽略。

## Office 文件与材料读取

- 企业保护的 Word、Excel、PowerPoint 文件可能由本机 Office 正常打开，而普通解析器无法读取。普通解析失败不等于文件损坏；乱码、空提取、文件名和上传成功均不算内容已读取。
- 仅处理用户已授权且企业策略允许的文件。先检查既有解析路径；该路径不可用时，可在本机验证已授权 Office 的只读自动化读取。禁用宏、不更新外部链接、不保存或改写原件，不关闭用户正在编辑的文档，不按进程名批量结束 Office。
- 分开记录人工打开、程序提取、页面渲染三种结果；每项注明来源文件身份及可核对位置。正文、表格、图片及其它对象逐类说明已取到、部分取得或未取得。Office 可读取不代表获准向模型或外部服务发送。
- 读取状态须区分已添加、读取中、已读取、部分读取、需要本机 Office 处理、权限受限。普通解析失败而原因未明时用“当前读取方式无法解析”；不猜内容、不把缺失资料算作已理解。
- 用户提及改扩展名只作为诊断线索，不实现批量改名、解密或保护绕过。本机读取组件限定 Word/Excel/PowerPoint 的普通解析和 Office 只读后备提取；新增格式、外发或视觉识别另行确定范围。
