import React, { useState } from 'react';
import { api, ApiError } from './api';
import { DocumentViewer } from './workbench';
import { useDialog } from './shell';
type Obj = Record<string, any>;
const statusNames: Obj = { read: '已读取', awaiting_vision: '待视觉分析', partial: '部分读取', failed: '读取失败' };
const kindNames: Obj = { requirement: '需求', rule: '规则', acceptance: '验收', goal: '目标', actor: '角色', constraint: '约束' };
const sourcePurpose: Obj = { current: '现状', goal: '目标', reference: '竞品参考', template: '模板' };
export function SourcePanel({ p, models, purpose, setPurpose, sourceText, setSourceText, url, setUrl, dynamic, setDynamic, busy, act, error, mutate, refresh, root, pendingGrant, setPendingGrant, guided = false }: { guided?: boolean; p: Obj; models: Obj; purpose: string; setPurpose: (s: string) => void; sourceText: string; setSourceText: (s: string) => void; url: string; setUrl: (s: string) => void; dynamic: boolean; setDynamic: (v: boolean) => void; busy: boolean; act: (fn: () => Promise<unknown>) => void; error?: string; mutate: (path: string, body: Obj, method?: string) => Promise<void>; refresh: () => Promise<void>; root: string; pendingGrant: Obj | null; setPendingGrant: (v: Obj | null) => void }) { useDialog(Boolean(pendingGrant), () => setPendingGrant(null)); return <><div className="section-heading"><h2>项目资料</h2><span>逐项保留读取状态和出处</span></div>{error && <p role="alert" className="error">{error}</p>}<div className="two-column"><section><div className="card"><label>材料用途<select value={purpose} onChange={e => setPurpose(e.target.value)}>{Object.entries(sourcePurpose).map(([k, v]) => <option key={k} value={k}>{v as string}</option>)}</select></label><textarea aria-label="粘贴材料" value={sourceText} onChange={e => setSourceText(e.target.value)} placeholder="粘贴零散材料，保留原文……" /><div className="toolbar"><button disabled={!sourceText || busy} onClick={() => act(async () => { await mutate('/sources/text', { text: sourceText, purpose }); setSourceText(''); })}>保存文字材料</button><label className="file-button">上传文档或截图<input type="file" accept=".txt,.md,.docx,.pdf,.png,.jpg,.jpeg,.webp" onChange={e => { const f = e.target.files?.[0]; if (!f) return; act(async () => { const data = new FormData(); data.append('file', f); data.append('purpose', purpose); data.append('expected_revision', String(p.revision)); await api(root + '/sources/file', 'POST', data); await refresh(); }); e.target.value = ''; }} /></label></div></div><div className="card"><label>公开网页 URL<input value={url} onChange={e => setUrl(e.target.value)} placeholder="https://…" /></label><label className="check"><input type="checkbox" checked={dynamic} onChange={e => setDynamic(e.target.checked)} />需要动态页面渲染</label><button disabled={!url || busy} onClick={() => act(() => mutate('/sources/url', { url, dynamic, authorized_public: true, purpose }))}>确认有权读取并添加</button><small>只读取公开网页；失败时可改用截图或导出文件。</small></div></section>{!guided && <aside><div className="card"><h3>材料发送授权</h3><p>材料会发送到所选模型接收端。先排除不应发送的材料，再授权当前范围。</p>{['model', 'vision'].map(slot => <div key={slot}><p>{slot === 'model' ? '主分析' : '视觉分析'}：{models[slot]?.base_url}</p><button onClick={() => setPendingGrant({ slot, origin: models[slot]?.base_url, source_ids: p.sources.filter((s: Obj) => !s.excluded).map((s: Obj) => s.id) })}>授权此接收端与当前材料</button></div>)}<small>新增材料后需要再次确认范围；密钥仅用于绑定接收端。</small></div></aside>}</div><div className="source-grid">{p.sources.map((s: Obj) => <article className="card" key={s.id} id={"source-" + s.id}><div className="section-heading"><h3>{s.title}</h3><span className="pill">{s.excluded ? '已排除' : statusNames[s.parse_status] || s.parse_status}</span></div><small>{sourcePurpose[s.purpose]} · {s.id}</small>{s.image_mime && <img className="source-image" src={'/api' + root + '/sources/' + s.id + '/image'} />}<p>{s.failure_reason}</p><details><summary>{s.excerpts.length} 个来源片段</summary>{s.excerpts.map((x: Obj) => <div key={x.id} id={"excerpt-" + x.id}><small>{x.locator} · {x.id}</small><p>{x.text}</p></div>)}</details><button onClick={() => act(() => mutate('/sources/' + s.id + '/exclude', {}))}>{s.excluded ? '恢复使用' : '排除材料'}</button><button onClick={() => act(() => mutate('/sources/' + s.id + '/retry', {}))}>重新读取（保留原件）</button></article>)}</div>{pendingGrant && <div className="modal-backdrop"><div className="modal" role="dialog" aria-modal="true" aria-label="核对材料发送授权"><h2>核对材料发送范围</h2><p>接收端：{models[pendingGrant.slot]?.name} · {models[pendingGrant.slot]?.model} · {pendingGrant.origin}</p><p>材料：{pendingGrant.source_ids.map((id: string) => p.sources.find((s: Obj) => s.id === id)?.title || id).join('、') || '当前没有未排除材料'}</p><div className="toolbar"><button onClick={() => setPendingGrant(null)}>取消</button><button className="primary" onClick={() => act(async () => { await mutate('/grants', { slot: pendingGrant.slot, source_ids: pendingGrant.source_ids }); setPendingGrant(null); })}>授权当前范围</button></div></div></div>}</>; }
export function VersionHistory({ history }: { history: Obj[] }) { return <><h2>版本记录与差异依据</h2><p className="muted">每次人工编辑及模型候选写入保留快照，旧基线不被覆盖。</p>{history.map((h: Obj, index: number) => { const snap = JSON.parse(h.payload), older = history[index + 1] ? JSON.parse(history[index + 1].payload) : null; return <article className="card" key={h.revision}><h3>v{h.revision} · {h.reason}</h3><small>{h.created}</small><details><summary>查看条目与上版变化</summary>{snap.items.map((i: Obj) => { const prev = older?.items.find((x: Obj) => x.id === i.id); return <div key={i.id}><b>{i.title}</b>{prev && prev.statement !== i.statement && <p className="muted">原：{prev.statement}</p>}<p>{prev ? '当前' : '新增'}：{i.statement}</p></div>; })}</details></article>; })}</>; }
export function ConfirmationPanel({ p, busy, setBusy, setError, setTab, setStage, setShowAdvanced, root, act, refresh, onReview }: { onReview?: () => void; p: Obj; busy: boolean; setBusy: (v: boolean) => void; setError: (s: string) => void; setTab: (s: string) => void; setStage: (s: string) => void; setShowAdvanced: (v: boolean) => void; root: string; act: (fn: () => Promise<unknown>) => void; refresh: () => Promise<void> }) { const [confirmationRequest, setConfirmationRequest] = useState<Obj | null>(null); const [confirmationError, setConfirmationError] = useState(''); useDialog(Boolean(confirmationRequest), () => setConfirmationRequest(null)); return <><div className="section-heading"><div><p className="eyebrow">确认与交接</p><h2>核对指定版本</h2></div><span>内容确认不代表开发或测试验收</span></div><div className="two-column"><section><article className="card"><h3>本期范围、规则与验收</h3>{p.items.filter((i: Obj) => i.selection_status === 'selected' && i.applies_to === 'to_be').map((i: Obj) => <p key={i.id}><b>{kindNames[i.kind]} · {i.title}</b><br />{i.statement}</p>)}</article><article className="card"><h3>重要待定与门禁问题</h3>{p.questions.filter((q: Obj) => q.blocking && q.status !== 'answered').map((q: Obj) => <p key={q.id} className="notice">{q.question}</p>)}{p.confirmation_issues.length ? p.confirmation_issues.map((issue: string, index: number) => <div key={index} className="notice">{issue} <button onClick={() => { if (issue.includes('文档') || issue.includes('PRD')) setTab('documents'); else if (issue.includes('线框') || issue.includes('页面')) setTab('prototype'); else { setTab('conversation'); if (issue.includes('审查')) { setStage('review'); setShowAdvanced(true); } } }}>前往核对</button></div>) : <p>程序门禁已满足。请人工核对业务意图与交付范围。</p>}<button onClick={() => { if(onReview) onReview(); else { setStage('review'); setShowAdvanced(true); setTab('conversation'); } }}>审查当前版本</button></article></section><aside><article className="card"><h3>当前确认对象</h3><p>底稿 v{p.revision} · PRD {p.documents.prd ? '已生成' : '未生成'} · UI {p.ui ? '已生成' : '未生成'}</p><details><summary>查看完整版本哈希</summary><pre>{JSON.stringify(p.hashes, null, 2)}</pre></details><p>确认后创建不可变基线。后续修改将成为新草稿。</p><button className="primary" disabled={busy || p.confirmation_issues.length > 0} onClick={() => { setConfirmationError(''); setConfirmationRequest({ expected_revision: p.revision, expected_hashes: p.hashes, scope_ids: p.items.filter((i: Obj) => i.selection_status === 'selected' && i.applies_to === 'to_be').map((i: Obj) => i.id), idempotency_key: crypto.randomUUID() }); }}>确认此版本的 PRD 内容</button></article></aside></div><h2>已确认基线与交接</h2>{p.baselines.map((b: Obj) => <article className="card" key={b.id}><h3>{b.id} <small>{b.id === p.active_baseline_id ? '当前确认基线' : '历史基线'}</small></h3><p>确认记录 {b.confirmation_id} · 底稿 v{b.project.revision}</p><button onClick={() => act(async () => { await api(root + '/exports', 'POST', { baseline_id: b.id, idempotency_key: 'handoff-' + b.id }); await refresh(); })}>生成此基线的研发交接包</button></article>)}{p.exports.map((e: Obj) => <p key={e.id}><a href={'/api' + root + '/exports/' + e.id}>下载交接包 · {e.baseline_id}</a>{e.baseline_id !== p.active_baseline_id && '（历史版本）'}</p>)}{confirmationRequest && <div className="modal-backdrop"><div className="modal" role="dialog" aria-modal="true" aria-label="确认内容复核"><h2>再次核对本期范围与重要未知</h2>{confirmationError && <p role="alert" className="error">{confirmationError}</p>}<p>底稿 v{confirmationRequest.expected_revision} · 已选条目 {confirmationRequest.scope_ids.length} 项</p><ul>{confirmationRequest.scope_ids.map((id: string) => <li key={id}>{id} · {p.items.find((i: Obj) => i.id === id)?.title || '原条目已变化'}</li>)}</ul><p>重要待定：{p.questions.filter((q: Obj) => q.blocking && q.status !== 'answered').map((q: Obj) => q.question).join('；') || '无待答阻塞问题'}</p><p className="notice">提交后由服务端核对版本、哈希、范围和门禁，不会自动重放 409 请求。</p><div className="toolbar"><button onClick={() => setConfirmationRequest(null)}>返回核对</button><button className="primary" disabled={busy} onClick={async () => { setBusy(true); setError(''); setConfirmationError(''); try { await api(root + '/confirmations', 'POST', confirmationRequest); setConfirmationRequest(null); await refresh(); } catch (e) { setConfirmationError(e instanceof ApiError && e.status === 409 ? '版本已变化：' + e.message + '。请重新核对当前内容后再确认。' : (e as Error).message); } finally { setBusy(false); } }}>提交服务端确认</button></div></div></div>}</>; }
export function DocumentStudio({ p, docType, setDocType, selectedDocId, setSelectedDocId, artifacts, setStage, setShowAdvanced, setTab, setOutcome, root, act, mutate, onGenerate }: { onGenerate?: () => void; p: Obj; docType: string; setDocType: (s: string) => void; selectedDocId: string; setSelectedDocId: (s: string) => void; artifacts: Obj; setStage: (s: string) => void; setShowAdvanced: (v: boolean) => void; setTab: (s: string) => void; setOutcome: (s: string) => void; root: string; act: (fn: () => Promise<unknown>) => void; error?: string; mutate: (path: string, body: Obj, method?: string) => Promise<void> }) { return <><div className="section-heading"><div><p className="eyebrow">需求文档</p><h2>文档阅读与导出</h2></div><div className="toolbar"><select aria-label="文档类型" value={docType} onChange={e => { setDocType(e.target.value); setSelectedDocId(''); }}><option value="prd">PRD</option><option value="mrd">MRD</option></select><button onClick={() => { if(onGenerate) onGenerate(); else { setStage('prd'); setShowAdvanced(true); setTab('conversation'); setOutcome('document'); } }}>{p.documents[docType] ? "更新讨论稿" : "生成讨论稿"} {docType.toUpperCase()}</button></div></div><p className="muted">切换文档不会调用模型。生成和重新生成是明确的模型任务；章节按对应 Word 的内容维度参考。</p><label>章节参考方式<select aria-label="章节参考方式" value={p.reference_mode || "user"} onChange={e => act(() => mutate("", { reference_mode: e.target.value }, "PATCH"))}><option value="user">用户原 Word 章节参考</option><option value="builtin">明确改用内置参考（原件不可用时）</option></select></label><div className="toolbar"><label>查看文档版本<select aria-label="文档版本" value={selectedDocId} onChange={e => setSelectedDocId(e.target.value)}><option value="">当前 {docType.toUpperCase()} 草稿</option>{(artifacts.document_artifact || []).filter((d: Obj) => d.content?.document_type === docType).map((d: Obj) => <option key={d.id} value={d.id}>{d.requirement_name || d.content.title} · v{d.draft_revision} · {d.created}</option>)}</select></label></div>{(() => { const document = selectedDocId ? (artifacts.document_artifact || []).find((d: Obj) => d.id === selectedDocId) : p.documents[docType]; return document ? <DocumentViewer key={document.id} document={document} items={p.items} currentRevision={p.revision} currentBriefHash={p.hashes.brief_hash} updateNeeded={p.stale_document_kinds?.includes(docType)} historical={Boolean(selectedDocId)} readiness={p.document_export_readiness?.[docType]} onClarify={() => setTab('clarification')} downloadRoot={'/api' + root} /> : <div className="empty">尚未生成 {docType.toUpperCase()}。</div>; })()}</>; }
