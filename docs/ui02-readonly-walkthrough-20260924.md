# UI-02 只读走查报告（2026-09-24）

> 结构说明：第一章为**源码与构建预查**（2026-09-24 上午）；第二章为**实际浏览器走查**（同日 12:40 起，服务重启后实测）。两章证据级别不同，结论以第二章标注为准。

本报告属于 **whj1677/requirements-agent**，走查对象为 UI-02 统一引导式工作区（PR #2，分支 `codex/ui-02-guided-workflow`）。与任何 ui-test-agent 的走查无关；本仓库 `docs/` 下既有报告（acceptance-report.md、human-review.md 等）均为本仓库历史交付记录，不是他项目报告。

# 第一章 源码与构建预查

## 前置核对结论

| 核对项 | 实际结果 |
|---|---|
| 仓库身份 | `D:\01_AI工程\01_工程项目\pm-req`，origin = `https://github.com/whj1677/requirements-agent.git`，为目标仓库本身，非按文件夹名猜测 |
| 分支 / HEAD | `codex/ui-02-guided-workflow` @ `53cc9f20191a7ab0aec091508fca96dd28a27c6c`（2026-09-24 10:57 +0800），与指定参考提交完全一致；工作区无未提交源码改动 |
| 工作区差异 | 仅 `.env.example` 删除（工作区未暂存）与未跟踪 `原型/`（均为 2026-09-23 既有状态，系 UI-02 状态卡中明确保护不提交的内容）；无 stash；与远端分支一致 |
| 实际服务地址 | `http://127.0.0.1:8765`（uvicorn，PID 21436，10:04:29 启动，HTTP 200）。4322/4331 为其他项目进程（PID 41528/25640），与本项目无关，未沿用旧端口假设 |
| 加载版本 | 服务返回的 index.html 引用 `/assets/index-Cnfdv_4i.js`，与 `web/dist/assets/index-Cnfdv_4i.js`（249101 字节，2026-09-24 10:55:33 构建）一致；`web/src` 无未提交改动，即服务当前提供的正是 HEAD 的 UI-02 最终构建（含确认弹窗错误位置修正） |
| 用户浏览器版本风险 | 数据库中唯一项目创建于 10:05:01（本地），早于 10:55:33 的最终重建。**若用户浏览器标签页自 10:05 起未刷新，其运行的是旧包，需普通刷新（F5）后再评审**；登录态在 Cookie 中，刷新不会清除会话。该标签页实际加载版本无法从服务端证实，这是本轮唯一未能闭环的核对点 |

## 走查方式与边界

只读执行：Git/进程/端口核对、SQLite 以只读模式打开、UI-02 既有证据截图（`evidence/runtime/ui02/663f7c76/`，与 HEAD 同构建）视觉走查、前端源码（workspace/guided/workbench/panels/shell/main/api）与后端契约（actions/main/contracts）逐行审读。

未执行：未触发生成/运行/正式确认，未调用 DeepSeek 等产品模型，未修改业务数据与页面，未登录真实服务（访问令牌仅存在于启动方控制台，不可获知），未改动 ui-test-agent 任何文件。渲染态真人体验仍为 NOT_RUN，本报告结论来自代码与同期证据，不等同真人首审。

走查对象工作流：提供零散资料 → 理解与澄清 → 比较讨论方向 → 核对需求 → 查看原型和 PRD → 理解确认交接条件。现有数据库仅一个空项目（无资料/条目/文档），不影响结构性走查结论。

## 重点检查结论

**1. 项目资料已融入统一工作区 —— 已解决。** 资料入口在工作区顶栏「项目资料 · N」与任务卡/助手区「添加资料」（`web/src/workspace.tsx:65,70`），打开为工作区内抽屉而非独立页面（`workspace.tsx:80`）；文本/文件/URL 添加、读取状态、排除/重读、来源片段定位均在抽屉内完成（`web/src/panels.tsx:9`；证据 `source-location.png`）。条目「查看来源」可自动展开对应片段并高亮（`workspace.tsx:35-39,62`）。残余：独立「材料发送授权」面板在引导模式下隐藏，授权并入任务弹窗，老用户找不到独立授权入口；「添加资料」与「项目资料」两处入口语义重叠。

**2. 不再需要在平级页面间寻找入口 —— 已解决。** 全应用仅单一工作区视图（`main.tsx:23`），五阶段「理解需求/推演方案/明确需求/文档评审/确认交接」为同页 hidden 切换（`workspace.tsx:66,72-76`）；项目切换为左侧常驻栏且已访问项目 keep-alive（`shell.tsx:36-37`、`main.tsx:23`）；模型设置与历史版本均有明确「返回当前任务」。无并列内容页残留。

**3. 用户不再必须理解内部 stage —— 已解决（默认路径）。** 用户可见处全部中文化（`guided.tsx:7` steps 映射、`workbench.tsx:5` stages 映射）；`workspace.tsx:15` 的 `useState('ingest')` 仅服务于折叠的「工程诊断与任务预算」面板（`workspace.tsx:78`）与内部回调，普通路径不可见。残余：诊断面板本质仍是 stage 选择器，仅靠 details 折叠与终端用户区隔；`guided.tsx:11` 的 `steps[s]` 无未命中回退。

**4. “生成”在当前区域执行并明确提示缺失条件 —— 已解决。** 各阶段主按钮直接发起对应业务动作（`workspace.tsx:60-61`：整理当前信息/探索可选方案/核对本期内容/生成或更新讨论稿/核对正式确认条件），方案卡、文档区内均有区域内二级入口。所有动作先经 `POST /actions/plan` 预检，弹窗展示底稿版本、请求总上限、接收端与授权状态、来源清单，缺失条件以业务语言逐条列出并禁用提交（`guided.tsx:17`、`app/actions.py:31-35,47,79`；证据 `authorization.png`）。残余：弹窗子步骤链偏工程视角；阶段 3 主按钮与文档区内按钮文案重复；无 Key 时入口按钮本身不置灰，仅靠 plan.missing 解释。

**5. 选择方向与采纳条目清楚分开 —— 已解决。** 方案卡上「按此方向继续讨论」与「采纳指定条目」为两个独立按钮（`workbench.tsx:44`；证据 `电价时段-preview-boundary.png`），弹窗分「记录讨论方向」（明示条目、假设、未答问题均不变）与「采纳指定关联条目」（逐条勾选、显示认知状态与来源原文、未勾选禁提交），走不同接口（`/options/{id}/direction` vs `/options/{id}/items`，`guided.tsx:23`），并共同提示「方向选择与条目采纳都不等于正式确认」。残余：「按此方向继续讨论」表述弱，用户可能不理解为选定方向；条目无全选；方向切换对已激活原型的影响不在弹窗内预告。

**6. 未决问题、讨论稿与正式确认容易区分 —— 已解决。** 未答问题集中在助手区「待你决定 · N」并区分关键/可后续决定（`workbench.tsx:11,26`）；文档明确标注「讨论稿可包含未决项」「生成/更新讨论稿」，原型标注「低保真交互 · 模拟数据」，无对应原型时明示不冒用项目原型（`workspace.tsx:73,75`；证据 `电价时段-draft.png`）；正式确认独立成阶段 5，确认按钮在门禁问题存在时禁用，旁注「内容确认不代表开发或测试验收」（`panels.tsx:11`；证据 `电价时段-confirmation-blocked.png`）。残余：进入文档正文阅读后，「讨论稿」性质仅靠顶部一行 notice，不在视野内。

**7. 返回、展开资料和切换阶段保留上下文 —— 部分解决。** 阶段切换保留主区滚动位置与各 section 内部状态（`workspace.tsx:20-21`，hidden 切换不卸载 DOM）；未发送输入跨阶段保留并明示「仅保留在当前会话」；任务弹窗「返回保留输入」、403/409 错误均不清输入（`guided.tsx:17`、`workspace.tsx:50-51,67`、`panels.tsx:11` 的 409 专处理）；资料抽屉草稿与跨项目 keep-alive 均保留。残余：助手区与抽屉内滚动不恢复；`checkedScope/expanded` 等附属状态跨阶段残留；来源片段高亮只加不除，旧高亮会残留。

**补充 8. 确认交接的缺口指引 —— 已解决。** 阻塞时逐条展示门禁 issue 并提供「前往核对」跳转（缺 PRD→文档评审、线框旧→推演方案原型、其余→讨论区），无缺口时提示「程序门禁已满足。请人工核对业务意图与交付范围。」（`panels.tsx:11`、`app/contracts.py:171-186`）。残余：跳转靠 issue 文案字符串包含匹配，文案改动会使按钮退化为跳讨论区，较脆；阻塞问题与「重要待定与门禁问题」卡信息重复。

**补充 9. 旧入口残留 —— 默认不可见。** 旧 stage 下拉仅存在于折叠的工程诊断面板；非引导模式的快捷按钮/「分析阶段」下拉、旧 ArtifactTabs 标签栏、旧授权面板均为当前不可达的死代码分支；全 src 无指向独立材料页的链接或路由。建议后续版本评估诊断面板是否继续对终端用户开放，并清理死代码。

## 总体结论

UI-02「单一工作区 + 五阶段业务导航 + 抽屉式资料/历史 + plan 预检弹窗」结构完整覆盖目标六步工作流，七项重点检查中六项已解决、一项（上下文保留）部分解决。主要残余 UX 薄弱点：方向选择语义强度（第 5 条）、附属状态与高亮残留（第 7 条）、缺口跳转的字符串路由脆弱性（补充 8）。

## 未执行与限制

- 真实渲染态浏览器走查 NOT_RUN：访问令牌仅存在于启动方控制台，且本轮约束不触发生成/确认；结论依据源码与 HEAD 同构建的既有证据截图。
- 用户浏览器当前标签页是否已加载 10:55 最终包未证实；若未刷新需先刷新再评审（见前置核对表）。
- 真实模型质量、真人体验与首审保持 NOT_RUN，与 `docs/UI-02-implementation.md` 状态一致，本报告不改变任何验收状态。

# 第二章 实际浏览器走查（2026-09-24 12:40 实测）

## 2.0 版本核对与服务重启事件

**重大发现（本轮浏览器实测）**：走查开始前，8765 服务处于**前后端混合版本**状态——后端进程 10:04:29 启动（uvicorn 无 `--reload`，`scripts/serve.py`），内存中是当时进行中的代码；此后 `core.py`/`store.py`（10:23）、`actions.py`/`provider.py`（10:47）、`workflow.py`（10:51）、`main.py`（10:52）陆续被修改，10:55 前端按 HEAD 重建。结果：浏览器加载**新前端**，调用的却是**旧后端**，点击项目后工作区无法加载，主区域仅显示裸文本 `"Not Found"`（证据 `00-enter-blocked.png`，接口 404 被直接渲染）。第一章中"后端运行版本未核实"的风险在此确认为实际故障。

**经用户授权，本轮执行了服务重启**（旧 PID 21436 结束，新 PID 31592 启动，SQLite 数据不受影响）。重启后：

| 核对项 | 结果 | 证据级别 |
|---|---|---|
| 工作区 HEAD | `53cc9f2` @ `codex/ui-02-guided-workflow`，源码无未提交改动 | 源码确认 |
| 服务提供的前端 | `index-Cnfdv_4i.js`，与 dist（10:55 构建）一致 | 本轮浏览器实测 |
| 标签页实际加载 | `index-Cnfdv_4i.js` + `index-DX8BTF8e.css`（performance 资源条目） | 本轮浏览器实测 |
| 后端运行版本 | 重启后新进程加载 HEAD 源码；`GET /api/projects/{pid}/actions` 由旧进程缺失（浏览器 404 实证）变为 200 | 本轮浏览器实测 |

登录方式：按用户指示不再人工输入；**登录校验本身未改动**，改为以本机临时令牌「（本机临时令牌，按任务要求不写入仓库）」由脚本自动登录（headless Chromium；该令牌字符串已于入库前脱敏，原文仅存于本机证据）。该令牌为临时本机用途，如需回收，用 `scripts/start.ps1` 重启即恢复随机令牌。用户自有浏览器标签页已随重启失效，需重新加载并用该令牌登录。

## 2.1 走查结果（A–F）

走查对象：现有项目「电价管理」（空项目，revision 0，无资料/条目/方案/文档）。全程未添加材料、未保存回答、未采纳内容、未发送输入。

| 项 | 预期 | 实际表现 | 证据级别 |
|---|---|---|---|
| A 进入项目 | 看懂阶段/结果/下一步；资料入口可见；无需诊断 stage | 五阶段业务导航正确显示；「项目资料 · 0」顶栏可见；空态引导文案完整（"先描述要解决的问题"）；诊断面板存在但默认折叠 | 本轮浏览器实测（`01-A`） |
| B 来源定位 | 条目→来源1→来源2→重开资料 | **未覆盖**：项目无条目/资料，且约束禁止添加材料 | — |
| C 上下文保留 | 输入/滚动/选中跨阶段保留；错误残留单列 | 助手输入跨阶段保留 ✓；资料抽屉草稿关开保留 ✓；Esc 关闭后焦点正确返回「项目资料」按钮 ✓；**滚动保留未获实证**（空项目页面无可滚动内容，scrollTop 设置后仍为 0） | 本轮浏览器实测（`02-C`、`03-C`） |
| D 方案与原型 | 只查看已有方案及对应原型；空态不借用 | **方案相关未覆盖**（无方案，未打开操作弹窗）；空态文案正确："尚无方案。数量由分析结果决定。" | 本轮浏览器实测（`05-D`），弹窗部分引用既有工程测试 |
| E 文档与确认 | 查看已有文档/缺口；「前往核对」到达正确区域 | 无文档（记录未覆盖，未生成模拟文档）；确认门禁正常：issue「缺少 PRD 内容对象；MRD 不能代替 PRD 确认」展示、确认按钮禁用、「前往核对」正确跳转「4 文档评审」；历史版本抽屉可开可返回 | 本轮浏览器实测（`06-E`–`09-E`） |
| F 操作预检 | plan 只读前提下打开并关闭 | 已先核对 HEAD 源码 `plan()` 无写库/无模型调用（`app/actions.py:21-65`）；实测弹窗展示底稿 v0、8 次请求上限、接收端、新增输入暂存区；点「返回保留输入」后输入仍在；**全程未点运行/授权/确认** | 本轮浏览器实测（`10-F`）+ 源码确认 |

## 2.2 问题清单（仅列有证据的）

1. **运行服务曾为前后端混合版本，新前端无法进入工作区**【本轮浏览器实测，已随重启恢复】。影响：10:55 后任何刷新页面的用户都会撞上 `"Not Found"`。建议最小调整：不改产品 UI，在工程流程上防止——`serve.py` 启动时检测 dist 构建时间并提示，或在 README/启动输出强调"代码或构建变更后必须重启服务"。
2. **接口 404 时前端直接渲染服务端原始错误体**【本轮浏览器实测】。"Not Found" 裸文本成为主区域唯一内容，无中文说明、无返回入口。虽是版本错配下的防御性场景，但任何接口缺失/路径错误都会以同样方式暴露。建议最小调整：项目加载失败分支统一为中文错误横幅 + 「刷新内容并保留输入」式操作。
3. **空项目下「生成讨论原型」「讨论修改方案」「查看项目当前原型」入口仍展示**【本轮浏览器实测，观察非缺陷】。条件拦截完全后置依赖 plan.missing；「查看项目当前原型」无原型时的空态本轮未覆盖（约束 D 只允许查看已有方案）。建议：先用真实数据复验空态，再决定是否前置置灰；本批不改。

未写成缺陷的项：诊断 stage 选择器仍存在但默认折叠（按任务约定不自动认定为阻塞）；滚动保留为覆盖缺口而非缺陷；F 中 missing 为空、授权按钮可用是正常行为（Key 已配置 deepseek-flash），缺失条件禁用态本轮未实测，引用既有工程测试（UI02-03/13）。

## 2.3 第一批建议改 / 不改

**改（小步）**：① 防混合版本的启动提示（工程侧）；② 接口失败的统一中文错误呈现（前端错误分支）。
**不改（本批）**：诊断面板与 stage 选择器；非引导模式死代码；「按此方向继续讨论」文案；前置置灰策略；确认门禁与预算逻辑。

## 2.4 尚未覆盖的场景

- B 来源定位与高亮清除（无条目/资料）；
- D 方向选择/条目采纳弹窗实际文案与取消行为（无方案；引用既有工程测试与第一章截图证据）；
- E 已有文档阅读、历史版本对照与下载（无文档；未生成模拟文档补齐）；
- C 滚动位置恢复（空项目无可滚动内容）；
- 缺失条件禁用态（本次输入已填且 Key 已配置）；
- 403/409 冲突（不制造版本冲突，引用既有工程测试 UI02-13）。

## 2.5 本轮账目

- 创建业务任务 **0**，模型请求 **0**；全部 POST 仅 2 次：登录 + `actions/plan` 只读预检。
- 走查后项目状态：revision 0、sources 0、items 0、tasks 0，与走查前一致（`walkthrough.json` final-state）。
- 登录建立的会话为服务内存态，不属于业务数据修改；服务重启经用户明确授权；固定本机令牌为临时措施，登录校验代码未改动。
- 截图存于 `evidence/runtime/ui02-manual-20260924/`（本机，evidence/ 不入库）。`10-F` 截图含模型接收端配置（api.deepseek.com · deepseek-flash，非密钥），需分享时应制作脱敏副本。未采集任何含 Cookie/请求头/令牌的网络归档。
