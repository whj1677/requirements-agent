# 已有平台增量需求：单机验证补充（2026-09-25）

> 历史验证记录：下文端口、原型流程和调用结果对应当时版本，原样保留。当前只使用根目录 8765 工作台和五步文档流程；启动见 [README](../README.md)，当前修复结果见 [验收记录](agent-repair-20260925.md)。不要从历史副本启动第二实例。

本次按用户补充的主场景验证：已有平台上增加功能。延续 `codex/ui-02-guided-workflow` / PR #2，起点 `4e512203f86a7f5652e94a1fd610783bb4e274b7`。使用隔离合成材料、独立数据和临时服务；原 8765、已交付 8766、原餐饮项目及全部历史账本不变。

## 改动与范围

- 摘要按现有 `applies_to` 分开平台现状、参考、本期条目；候选显示适用范围和真实性属性。“草稿已采纳”不等于新增功能或正式批准。约束不一概改名为保持不变。
- 理解、方案、澄清、原型和成文提示聚焦增量：沿用已知角色、流程、字段和权限，明确本次新增、保留、不涉及与待核对影响。不新增平台类型或工作流引擎。
- 快速连续采纳曾造成旧 revision 冲突；提交期间禁用条目采纳控件，保留服务端版本校验。未自动重放 409。
- 真实浏览器发现 select 的“全部”被当作普通查询值，结果为零条。协议增加可选 `filter_all_option`，校验其适用组件与选项集合；解释器只按明确声明取消限制，不硬编码“全部”字样。select 精确匹配，text 仍包含匹配。旧 spec 不被重写。
- 非法 JSON 的修复指令现在含实际行、列、字符位置；不清洗失败输出。UI 上下文不再重复发送整份 PRD 和历史结构化响应，保留当前全部条目、问答、方案、活动 UI、用户摘要和材料。最小外壳示例经过实际 Schema 校验，绝不作为失败回退。

## 正常浏览器中的真实模型过程

预写合成材料：已有设备运维平台与工单列表，在原三列上只增加状态下拉和按钮；原导航、角色及权限保持不变，不新增办理、编辑、导出等功能。没有把评判预期当成真实用户业务需求。Codex 通过应用输入及点击；没有直接写底稿、数据库或模型产物。

1. 理解 1 次，形成 13 条、3 个未答问题。模型正确区分现状与新增，但重复追问已给出的按钮触发和恢复路径，记录为语义/易用性缺陷。
2. 通过正常条目控件仅选择 1 条现状、1 条新增需求；其余 11 条和全部问题保留原状态。连续点击时的 409 保留事实；刷新保留输入后按单项重试。
3. 初版原型 2 次（Schema 修复 1 次）保存；真实操作复现“全部筛选变空”，不把保存成功当功能成功。
4. 修正筛选协议后的候选任务连续 3 次非法 JSON，未保存候选。增加错误位置后的独立任务又 2 次失败；旧原型仍保留。
5. 原型失败期间，PRD 独立 1 次保存为 v4，导出 Word；该文档未冒充原型已修好。
6. 去除重复上下文并提供结构示例后的新任务，2 次（修复 1 次）保存候选。真实浏览器验证：全部 3 行→处理中 1 行→全部 3 行；只读角色筛选待处理为 1 行。未增加编辑表单或无关模块。
7. 采纳候选后主 iframe 内容身份即时变化，底稿由 v4 到 v5；全部条目和问题快照相等。旧 PRD 显示过期且无下载入口。
8. 另一次 PRD 更新 1 次保存为 v5：`DOC-f1f87ec548764ff7`。同一保存对象导出 Markdown/Word。未创建正式确认或基线。

不能把这些尝试拼成“一次直接成功”。最终 UI 仍经过一次格式修复；首次理解产生的重复问题未自动删除或假装回答。

## 内容与产品判断

最终正文 8 个组织章节：文档目的与资料说明、产品背景与现状、本次目标与范围、用户角色与权限、使用流程与页面方案、功能详细描述、数据与异常处理、非功能性需求。另保留问答、限制及历史记录。

正文能识别本次仅改列表筛选，既有角色、权限和办理流程不属于重建范围；候选/未决身份保留，没有正式批准记录。正常浏览器及 Word 中存在真实生成内容和对应原型插图。

剩余限制：模型仍可能重复询问材料已说明的规则，文档会继承这些待定标记；简单改动的正文和来源/历史说明仍偏长，需 PM 审阅。现有平台视觉材料未提供，本次只验证局部低保真原型，不能声称复刻任意平台样式。产品定位为单机讨论与审稿工具，不能把结果直接视为业务验收或免审研发规格。

## 请求账本

本增量场景单独 12 次真实请求，5 次为修复；不是原餐饮 15 次、pilot-01 7/30、LIVE-01/LIVE-02 或前一份单机报告的重算。全部 DeepSeek 官方 / deepseek-flash，单次 max_tokens=16000；每任务上限明确设置，未自动扩容。

供应商 usage 合计 prompt=233637、completion=40879、total=274516；逐次请求耗时合计 140.904 秒，非全流程墙钟耗时；费用未知。上轮单机报告的 14 次、5 次修复独立保留。

有记录的 UI 初次请求 prompt usage 从去重前 37649 到去重后 11940；序列化历史由 35277 字符到 1778，当前上下文其余部分由 34156 到 12558。两次发生在不同代码版本，不能据此声称统计成功率或普遍费用收益。

## 实际命令及结果

除前端命令在 `web` 外，工作目录均为仓库根目录。

| 命令 | 退出码 | 实际结果 |
| --- | --- | --- |
| `.venv/Scripts/python.exe -X utf8 -m pytest tests/test_ui_request_contract.py tests/test_pilot01_fix.py tests/test_preview_usability.py -q` | 0 | 最终目标回归 17 项；先补的全部筛选/Schema、上下文去重及错误位置断言最初确实失败 |
| `.venv/Scripts/python.exe -X utf8 -m pytest tests -q` | 0 | 最终完整回归 156 项，2 项依赖弃用警告；包括 R1–R3、密钥、版本与 Word 相关回归 |
| `npm run build -- --outDir ../evidence/runtime/incremental-20260925/web-final/dist` | 0 | 隔离构建，JS `index-C50BvpRR.js`；未覆盖 8765/8766 的 dist |
| `.venv/Scripts/python.exe -X utf8 scripts/check_ui02_browser.py --web-dist evidence/runtime/incremental-20260925/web-final/dist` | 0 | 电价与联系人正常 UI 合成 HTTP 场景；12 次本机夹具请求，0 页面脚本错误，覆盖提交中禁止重复采纳及 1440/1280/1024/800 宽度 |
| `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/render_word.ps1 -DocumentPath evidence/runtime/incremental-20260925/verified/PRD.docx -OutputDirectory evidence/runtime/incremental-20260925/verified/word-pages` | 0 | 最终 Word 实际渲染 10 页；此前 v4 Word 为 9 页，分别保留 |
| `git diff --check` | 0 | 无空白错误 |

中间工程执行失败也保留：一次 npm 在根目录误执行而非 web（ENOENT）；不限定目录的 pytest 收集到了本机分发副本并发生模块重名，随后完整执行仓库 `tests`；浏览器新延迟夹具曾提前解除路由，修正等待路由完成后重跑；新增测试最初漏导入 copy，补齐后才验证实际红/绿结果。均不改历史断言或掩盖模型失败。

本机证据在 `evidence/runtime/incremental-20260925/`，按修复前及 verified 分开。最终合成浏览器报告为 `evidence/runtime/ui02/ee33c41f/results.json`。原始输入输出、截图、数据库、Word 均不上传公开仓库。

NOT_RUN：真实平台截图/设计系统还原、用户真人内容首审、跨机器首次安装、长期稳定性、多用户/公网部署。没有执行生产业务请求、正式确认、合并或部署。

最终文案修订仅将方向状态显示为‘已选定’，避免与条目采纳混淆；再次隔离构建到 web-release/dist（同一 npm 命令仅替换 outDir），退出码 0，JS index-3Z4YFMz1.js。Word 最终 10 页总览已实际查看，中文、标题与第 3 页原型图/图注无主要裁切；历史说明占后段篇幅，偏长问题保留。
