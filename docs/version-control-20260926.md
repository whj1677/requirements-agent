# Git 归档与正式版本（2026-09-26）

## 任务状态

- 当前阶段：归档当前工程、从干净检出验证、合并正式分支。
- 唯一目标：让已验收实现及其回归可由 Git 完整恢复，并用 `main` 和固定发布标签标识正式源码。
- 输入：当前工作区、远端分支、`snapshot-10` 与已验收发布包清单、原始测试记录。
- 交付：完整工程提交、合入测试摘要、正式分支与标签、此版本记录。
- 验收：必要实现和测试均已跟踪；干净检出完成自动回归和前端构建；敏感运行资料未进入提交；远端正式引用与本地一致。
- 范围与停止：仅完成版本归档及必要验证；不重跑付费模型，不修改日常业务数据，不扩展产品功能。满足上述条件后停止。

## 正式版本约定

正式源码分支为 `main`，单机本地发布标签为 `v1.1.0-local.20260926`。标签固定本次代码；以后 `main` 可以继续接受验证过的更新。历史开发分支不作为另一个正式入口。

开始整理时，`main` / `origin/main` 为 `085d965`，是开发分支 `85efaba` 的祖先，落后 27 个提交；`codex/test-records-release-20260926` 为 `96d5d37`，只增加一份摘要。源码修改和新文件尚未提交，所以这些旧提交都不能单独代表已验收工作区。

## 受控范围

- 纳入当前源码、提示与契约、运维脚本、自动回归及浏览器验证脚本、`pytest.ini`、历史诊断和发布说明。
- 纳入合成 `原型/` 与展开的 UI 设计交接资料，原文件保持原样；只作为历史参考，不能替代当前业务规则、运行界面或验收结果。
- 按用户现有工作区状态记录 `.env.example` 的删除；README 已提供不覆盖已有 `.env` 的本机创建步骤。
- 6 个读取本机调用记录的一次性探针原样归档至忽略目录 `evidence/git-control-20260926/local-probes/`；重复交接 ZIP 与展开目录内容一致，精确忽略并保留原 ZIP。
- `.env`、日常 `data/`、原始模型证据、备份、依赖及构建产物继续本地保留；不加入公开 GitHub 仓库。

## 与既有验收的关系

归档前重新核对：`snapshot-10` 中的 64 个文件、已验收包清单中的 80 个文件与当前工作区逐字节一致。旧包 ZIP 的 SHA-256 为 `e87a8fc3f8167e0584a78ef52dda8e698dbb86c263ee9625c11e47730d35da02`。旧清单保留 `source_commit=85efaba`、`working_tree_dirty=true` 的原始身份，不改写成干净提交。

旧全量结果为 318 项、0 失败、0 错误、0 跳过。该历史快照未记录测试文件哈希；本次将实际测试源码纳入 Git，并从干净检出重新验证。正式源码比较允许 Git 的 CRLF/LF 文本换行转换；真实模型和界面验收仍对应原发布记录，不冒充在新提交上重新执行。

详细既有结论见 [单机发布验收](release-readiness-20260925.md)。它只适用于既有本机环境与合成验收；不扩大到新机器安装、公网、多用户或真实业务批准。

## 本次验证与合并

结果将在干净检出验证与远端核对完成后写入；不得把本节的计划视为已执行结果。
