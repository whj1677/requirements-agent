# Git 归档与正式版本（2026-09-26）

## 任务状态

- 当前阶段：完整工程已归档，干净检出验证完成，正式交付使用 `main` 与下述固定标签。
- 唯一目标：让已验收实现及其回归可由 Git 完整恢复，并用 `main` 和固定发布标签标识正式源码。
- 输入：当前工作区、远端分支、`snapshot-10` 与已验收发布包清单、原始测试记录。
- 交付：完整工程提交、合入测试摘要、正式分支与标签、此版本记录。
- 验收：必要实现和测试均已跟踪；干净检出完成自动回归和前端构建；敏感运行资料未进入提交；远端正式引用与本地一致。
- 范围与停止：仅完成版本归档及必要验证；不重跑付费模型，不修改日常业务数据，不扩展产品功能。满足上述条件后停止。

## 正式版本约定

正式源码分支为 `main`，单机本地发布标签为 `v1.1.0-local.20260926`。标签固定本次代码；以后 `main` 可以继续接受验证过的更新。历史开发分支不作为另一个正式入口。

后续 Windows 客户安装候选版 `1.2.0` 已纳入 `main`，最终构建固定提交 `82d89ac92b87c071bd13e359568bf72a612e6aa7`。该包已完成干净 Windows CI 的真实未授权安装/退出/卸载；用户要求保持日常工作台运行，最终包本机网页授权与 Office 复验延期。它仍是候选包，未创建正式 Windows 发布标签、未移动既有 `v1.1.0-local.20260926` 标签。具体哈希和验收归属见 [Windows 安装包验收记录](windows-distribution-acceptance-20260926.md)；后续仅增加报告的提交不改变该安装器的源提交身份。

开始整理时，`main` / `origin/main` 为 `085d965`，是开发分支 `85efaba` 的祖先，落后 27 个提交；`codex/test-records-release-20260926` 为 `96d5d37`，只增加一份摘要。源码修改和新文件尚未提交，所以这些旧提交都不能单独代表已验收工作区。

## 受控范围

- 纳入当前源码、提示与契约、运维脚本、自动回归及浏览器验证脚本、`pytest.ini`、历史诊断和发布说明。
- 纳入合成 `原型/` 与展开的 UI 设计交接资料，原文件保持原样；只作为历史参考，不能替代当前业务规则、运行界面或验收结果。
- 按用户现有工作区状态记录 `.env.example` 的删除；README 已提供不覆盖已有 `.env` 的本机创建步骤。
- 6 个读取本机调用记录的一次性探针原样归档至忽略目录 `evidence/git-control-20260926/local-probes/`；重复交接 ZIP 与展开目录内容一致，精确忽略并保留原 ZIP。
- `.env`、日常 `data/`、原始模型证据、备份、依赖及构建产物继续本地保留；不加入公开 GitHub 仓库。

## 与既有验收的关系

归档前重新核对：`snapshot-10` 中的 64 个文件、已验收包清单中的 80 个文件与当前工作区逐字节一致。旧包 ZIP 的 SHA-256 为 `e87a8fc3f8167e0584a78ef52dda8e698dbb86c263ee9625c11e47730d35da02`。旧清单保留 `source_commit=85efaba`、`working_tree_dirty=true` 的原始身份，不改写成干净提交。

旧全量结果为 318 项、0 失败、0 错误、0 跳过。该历史快照未记录测试文件哈希；本次将实际测试源码纳入 Git，并从干净检出重新验证。正式源码比较允许 Git 的 CRLF/LF 文本换行转换；真实模型和界面验收仍对应原发布记录，不冒充在新提交上重新执行。

详细既有结论见 [单机发布验收](release-readiness-20260925.md)。它只适用于既有本机环境与合成验收；不扩大到新机器安装、公网、多用户或真实业务批准。

## 本次验证与合并

已执行的验证对象为干净检出 `3992c8cbdaa904ba32a25aa11fa4cd5f525e371e`，包含完整工程提交 `748ef3d` 与原摘要提交 `96d5d37`；之后仅补充本次归档和历史报告的版本说明，不改变实现或测试。

| 核对项 | 实际结果 |
|---|---|
| 干净检出自动回归 | 本机 Python 环境执行 `pytest tests -q`，启用 `RA_TEST_LOCAL_OFFICE=1`，数据目录隔离；318 项通过、0 失败、0 错误、0 跳过，126.96 秒，2 条既有弃用警告 |
| 前端依赖与构建 | 从锁文件执行 `npm ci --offline --no-audit --no-fund` 及 TypeScript/Vite 构建，退出 0；未使用工作区外的未跟踪前端源码 |
| 历史构建对比 | JS/CSS 与原验收构建字节一致；`index.html` 仅存在 Git CRLF/LF 换行差异，规范化后相同 |
| 源码完整性 | 154 个实现、脚本、测试及构建输入与主工作区内容一致，差异仅为 Git 换行转换 |
| 干净源码打包 | 80 个文件逐项哈希校验；`source_commit=3992c8c…`、`working_tree_dirty=false`、`included_untracked=[]`；重新构建一致并通过包内离线会话/页面检查 |
| 归档安全 | 最终工程文件清单及文本凭据扫描未发现应排除的运行资料或真实凭据；合成设计图片与原始本机资产一致 |
| 数据不变性 | 整理前后 9 个日常项目 payload 哈希完全相同 |
| 本轮新增模型调用 | 0；真实模型及浏览器语义结论引用既有发布验收，不重复计数 |

本次干净源码包 SHA-256：`07d554eae871453dc627e8ed3518df792396827161166d3d562881705c5b49d3`。它与旧工作区包分别保留，未覆盖旧证据。详细机器结果留在本机忽略目录 `evidence/git-control-20260926/`。

`main` 合入完整开发历史与测试摘要；原 PR #2 的目标从历史原型分支调整为 `main`，避免将新版继续挂在旧原型分支下。正式版本以 `main` 和标签 `v1.1.0-local.20260926` 的实际 Git 引用为准；不删除历史分支、不强推或重写已有提交。

版本受控与运行进程是不同状态。本次只完成 Git 整理和隔离验证，未启动或重启日常工作台；已有前端及本机业务数据保留。干净检出仍复用了本机 Python 运行环境与已安装 Office，不据此声称新电脑安装已验收。
