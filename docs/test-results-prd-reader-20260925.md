# PRD 阅读、需求命名与下载澄清门槛修正

## 范围与结果

基于 `b84733f8d5cfebb347b16108ac9ea90d70a89f27`，继续现有 `codex/ui-02-guided-workflow` / PR #2。
本批只修正文阅读、命名及下载前澄清，不改变条目采纳、问题答案、正式确认或旧试跑事实。
停止条件是正常入口可阅读正文，有未答问题时不能下载，回答并更新文档后下载正确命名的文件。

- 阅读页与公开 Word/Markdown 使用同一份阅读内容：规范原文保留；编译器生成的来源 ID、英文状态、重复免责声明、模板映射清单和历史运行说明不再混入正文。内部原始内容、来源、映射和历史产物仍保留。
- 候选、推断、未决问题及回答与旧条目的差异仍有中文标记。只转换已识别的程序说明，不任意删改业务原文。既有模型写出的冗长段落不回填重写。
- 新文档保存当时的需求名称和问答快照；旧文档从对应历史快照补足展示信息。标题、版本选择与文件名含需求名称；重命名项目不改变旧文档名称。
- 所有公开下载格式均校验未答问题，包括非关键问题；返回具体问题。回答后旧文档仍不可下载，必须更新；UI 候选激活导致文档过期的限制继续保留。
- 下载链接携带实际文档 ID，服务端发现当前文档已换版则拒绝，避免同底稿版本串文档。历史文档继续只读。
- “去澄清这些问题”打开问题区并定位输入；从展开阅读和窄屏模式进入也恢复可见的助手区。
- 成文提示补充具体需求标题、避免重复引用原文、避免输出内部 ID 和运行过程的要求。契约、引用校验、固定输出预算未放宽。

## 实际验证

工作目录：`D:\01_AI工程\01_工程项目\pm-req`；构建工作目录为其 `web` 子目录。

| 命令 | 退出码 | 结论 |
|---|---:|---|
| `.venv/Scripts/python.exe -m pytest tests/test_document_publication.py -q`（修复前） | 1 | 3 项失败，检出缺少阅读内容、未澄清仍可下载、未绑定文档 ID |
| 同一目标命令（首轮修复后） | 0 | 原 3 项通过 |
| `.venv/Scripts/python.exe -m pytest tests -q`（最终） | 0 | 160 项通过，含候选/推断身份新增回归；2 项依赖弃用警告 |
| `npm run build -- --outDir ../evidence/builds/prd-human-20260925` | 0 | 隔离构建；最终 JS 为 `index-tR8n4sEb.js`，没有覆盖 8765 的 dist |
| `.venv/Scripts/python.exe scripts/check_ui02_browser.py --web-dist evidence/builds/prd-human-20260925` | 0 | 最终证据 `evidence/runtime/ui02/9313cc30/results.json`；电价/联系人两类正常入口，14 次本机合成 HTTP 请求，浏览器控制台错误 0 |
| `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/render_word.ps1 -DocumentPath <合成下载的联系人.docx> -OutputDirectory <本机word-pages>` | 0 | Word 实际渲染 1 页，已查看中文、标题、正文与页脚，无裁切；此样本不含表格 |
| `git diff --check` | 0 | 未发现补丁空白错误 |

浏览器完整路径：生成未澄清草稿 → 下载入口不可用且直接请求返回 409 → 进入澄清 → 输入预先固定的合成回答 → 原文档过期 → 更新文档 → 下载中文需求名称文件 → 故意让下一次生成失败 → 原文档仍保留。保留原方案方向/采纳分离、材料失败恢复、项目切换、各桌面宽度与窄屏回归。

中途失败独立保留：第一次脚本等待了折叠区域内不可见的答案摘要，改为检查外层已回答状态与实际保存值；第二次浏览器检出中文文件名回退，原因是 `filename*` 的 UTF-8 分隔符缺失，已修正并新增精确断言。未改写原报告。另一次编辑命令误在 `web` 工作目录调用根目录 Python 路径，编辑未执行，已在根目录重做。

## 边界

本批外部真实模型请求 **0**；新提示对真实模型成文风格的效果为 **NOT_RUN**，工程机制和合成浏览器验证不能替代它。
已用既有工单项目的一致性副本检查实际阅读页、3 项未答问题和跳转，不修改原项目的答案、选择或产物。
本批 Word 样本的表格检查为 **NOT_APPLICABLE**；完整真实业务 PRD 的内容首审与审批为 **NOT_RUN**。
下载得到需求评审稿，不创建人工批准或交接基线。候选内容仍保持身份，不能将“可下载”解释为全部业务已批准。
私有副本、截图、Word、调用记录均只在忽略的本机证据目录；历史真实失败及各次账本不变。
