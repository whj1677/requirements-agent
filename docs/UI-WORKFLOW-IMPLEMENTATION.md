# UI 工作流改造：设计核对与实施记录

## 当前任务卡

- 阶段：M0/M1 已核对并获授权，M2 已在现有应用实施，M3 离线工程与浏览器验证完成；用户体验验收待进行。
- 唯一目标：在现有工作台完成清楚的六步工作流，优先 P01–P03，移除常驻通用聊天栏。
- 输入：用户指定的 UI_DESIGN_HANDOFF.md、五张概念图、当前代码与实际页面。
- 交付：本记录、原应用内的页面及状态、相关回归和真实浏览器操作证据。
- 不做：MRD/PRD 写作算法重构、DSH、业务 UI 设计器、重新编号、另一个演示应用。
- 边界：保持一个日常工作台、现有数据和未提交修改。预检没有外部调用；实施后按用户授权启动/关闭隔离检查实例，并备份更新 8765；本轮外部模型请求为 0。
- 停止条件：实现设计与关键状态、验证实际路径、恢复唯一入口后交付，不宣称业务审批或正式发布。

## 1. 基线与已核对事实

2026-09-25，分支 `codex/ui-02-guided-workflow`，HEAD `d071bac3880174d35f53ace8fc6aebb3c3933301`。
唯一监听地址为 `http://127.0.0.1:8765/`。服务启动记录 runtime_id 为 `94242aa1c9d6a55a`，前端 `index-B9XW6sc2.js` / `index-DAjTi8EO.css`。

交接包五图已按 P01–P05 顺序逐张查看，尺寸均为 1672×941，SHA-256 均与 assets-manifest.json 一致。原图未修改。新交接包/压缩包与已有未提交内容不擅自纳入提交。

浏览器从 8765 正常入口只读打开已有空项目，实际看到三个空核对字段、保存核对说明、继续前缺口，以及另一处“本轮想讨论的内容”和“发送并整理”。双输入及先出现空核对表得到当前页面证据，不仅是设计推断。

## 2. 当前实现与目标接线

| 页面/能力 | 当前事实 | 本轮处理 |
|---|---|---|
| 框架 | 项目侧栏与六步导航已有；Workspace 永久挂载 ConversationPanel | 保留侧栏/导航，改为一个主内容区；辅助帮助按对象打开 |
| P01 | 空项目即显示 ProductCheckpoint 三字段，同时常驻助手可发送；organize 已可处理零散输入 | 统一一份任务输入；分析后展示结果，进入 P02 核对，不重复填两套表 |
| P02 | 七字段常驻文本框，下面以方案为主；独立需求范围选择已有 | 四类可读结果卡，按需编辑；相关人物/价值/优先级等在有缺口时展开；需求清单与明确采纳入口靠近对象 |
| P03 | 问题回答在助手，行为/范围说明在主区；单题保存与 scope API 已有 | 问题按需求分组，阻塞优先；未保存/已保存/建议/待决定分开；保存回答与继续是不同动作 |
| P04 | 原图默认折叠，草图也需另开；核对表排在图前 | 默认对照现状/草图；无原图明确空态；1024 下切换查看；结果后展示变化、保持和核对动作 |
| P05 | 同源文档/历史身份/导出门禁已有；大核对区排在正文前；只有合并阶段核对 | 正文优先，双文档标签、独立状态及阅读位置；补每份文档版本对应的显式评审记录 |
| P06 | ConfirmationPanel、confirmations、baselines、exports 已实现 | 重组范围、交付物、阻塞和确认栏；逐项版本/查看入口；包未生成、生成中、失败、可下载分开 |
| 阶段门禁 | product_flow 提供 available/missing/needs_recheck，写入核对检查 hash/revision | 复用；第一步“输入已接收”和第二步“理解已核对”语义需按 D1 最小对齐 |
| 状态失败 | 已有部分错误分类和旧结果保留；辅助请求回写早于 serial 校验；部分写动作只看 busy | 修正当前响应归属、状态新鲜度及提交时复核，不建立第二套状态真相源 |
| 来源定位 | 已有资料抽屉及 excerpt 定位，但旧高亮没有完整清理 | 对象化入口、正确定位/关闭恢复；不可定位如实说明 |
| 新增候选 | 当前有修改/采纳现有条目的 API，没有手工新增条目 API | 最小候选创建接口：程序分配稳定 ID、用户输入来源、初始 candidate；不自动采纳 |
| 局部帮助 | 当前 action message 没有可靠的需求/页面/章节作用对象约束 | 复用 actions/plan 和现有阶段；最小绑定对象与版本，修改返回候选，不直接改正文 |
| 能力限制 | 文件实际上限为 12 MiB；MRD/PRD 模板内容参考已有 | 显示真实支持能力，不照抄 50MB，不加模板评分引擎 |

关键实现位置：`web/src/workspace.tsx`、`product-flow.tsx`、`workbench.tsx`、`panels.tsx`、`guided.tsx`、`shell.tsx`、`app/product_flow.py`、`app/main.py`、`app/actions.py`。写作引擎不在本轮重构范围。

## 3. 集中核对的产品取舍（已获得实施授权）

| 决定 | 推荐设计 | 对现状的影响 |
|---|---|---|
| D1 开始门槛与重复核对 | “本次想新增或修改什么”为核心输入，平台/页面与保持项可补充；可先只提供材料。分析后进入 P02 查看理解，缺失继续显式显示；真正的范围核对留在 P02 | 不再要求第一步先填齐独立 product/module/intent 核对表；调整第一步接收输入的检查语义，不能因此跳过 P02/后续门禁 |
| D2 局部 AI 入口 | 默认关闭的“帮我完善此项/调整此草图”抽屉，标题显示对象与版本；一处修改意图输入，结果为候选差异。完整对话保留在历史/任务记录 | 移除永久助手栏和全局发送入口；方案比较是范围页的可选展开能力，不强制每个项目探索方案 |
| D3 双文档评审 | MRD、PRD 分别点击“已核对这份文档”，绑定实际 artifact ID 和当前内容；两份均有效且已核对，才“完成文档评审并继续” | 新增最小评审记录及门禁接线；旧整体阶段核对保留原语义，不补造过去的逐文档评审事实 |
| D4 未保存内容 | 离开编辑时提供“留在当前页/保存后离开/放弃修改”；保存只保存当前输入或回答，不启动模型。无法持久化的控件明确“仅本次页面保留” | 按实际对象保存；不默认把私有正文塞入浏览器持久化缓存；刷新给离开提示，不承诺跨重启恢复 |

其余工程选择自主完成：延续灰白/绿色；一个主要滚动区；窄屏折叠步骤条；字段/来源/版本绑定及焦点管理。以上确认不等于业务条目批准、模型调用授权或部署授权。

## 4. P06 补充结构稿（设计，不是运行截图）

```text
项目名称 · 当前内容状态                 项目资料  历史版本  更多
① 提供资料 → ② 范围 → ③ 规则 → ④ 草图 → ⑤ 文档 → ⑥ 确认与交接

确认与交接
请核对这次确认的需求范围与版本。

┌ 本次需求范围 ──────────────────────────────────────┐
│ 实际需求编号 / 名称 / 内容版本 / 新增、修改或保持     │
│ 原文摘要                              查看需求详情 │
└───────────────────────────────────────────────────┘
┌ 本次交付物 ─────────────────┬ 当前确认对象 ─────────┐
│ MRD       实际版本、状态 查看│ 当前底稿及版本         │
│ PRD       实际版本、状态 查看│ 本次实际范围           │
│ 需求草图  实际版本/不适用    │ 展开版本依据           │
│ 研发与验收资料  真实可用性   │                        │
└────────────────────────────┴───────────────────────┘
┌ 仍需处理 / 保留的未知 ───────────────────────────────┐
│ 阻塞原因 → 前往具体对象；非阻塞未知单列并保留理由     │
└───────────────────────────────────────────────────┘
确认的是此版需求，不代表代码已实现、测试通过或最终 UI 已批准。
返回文档评审              当前缺口说明       [确认此需求版本]
```

无前置条件时主按钮禁用但解释可见；点击可用按钮后弹出实际范围/版本复核。403 保留内容并显示拒绝原因；409 显示旧请求版本与最新差异，提供“读取最新内容并重新核对”，不自动重放。成功后显示确认记录、基线身份，再开放“生成此基线交接包”；生成失败保留基线，不能再次确认冒充重试。下载只指向已存在且匹配基线的包。

## 5. 关键状态稿与按钮

以下为局部状态布局，方括号是按钮，尖括号是实际数据占位，不是产品默认业务值。

```text
P01 / 分析前
  本次想新增或修改什么？  [核心输入]
  [补充平台/页面与保持项]   已入项目资料 / 待上传分别显示
  输入未保存              [分析现状与诉求]

P01 / 处理中
  正在 <服务端真实阶段> · 本次输入与资料可查看
  [停止后续步骤]           不显示虚构百分比，不重复发任务

P01 / 部分结果或失败
  <实际原因>   已取得的结果 / 未读材料分别显示
  [查看已有结果] [核对后重试] [返回保留输入]

P02 / 结果与编辑
  现状摘要  改动目标  保持项  不在本期范围  各卡 [编辑][来源]
  编辑中：<原内容与本次修改> [保存] [取消]
  下方：真实缺口、需求清单   [确认范围并继续]

P03 / 未保存回答
  <需求编号与名称> → <问题> → 影响说明
  建议（如果实际存在） [使用建议] → 只填草稿
  已填写，未保存           [保存回答]

P03 / 保存成功仍阻塞
  回答已保存；<实际剩余缺口> [前往处理]
  已保存回答 / 待核对规则 / 仍未知分区，继续不可执行

P04 / 草图缺失或过期
  原页面（或缺失说明） │ 当前草图（或旧版只读标识）
  <更新原因与关联编号>  [生成/更新草图] [查看旧版]
  当前有效且已核对时   [确认草图并继续]；不自动生成文档

P05 / 双文档部分失败
  [MRD · 已保存、待评审] [PRD · 本次失败]
  成功文档正文保留；失败文档显示实际原因与旧版入口
  [核对后重试失败任务]；MRD 不跟着重复生成

公共 / 未保存离开
  有未保存的 <对象> 修改
  [留在当前页] [保存后离开] [放弃修改]

公共 / 版本冲突
  本次编辑基于 <旧版本>，服务端现为 <新版本/尚未取得>
  编辑保留；[读取最新内容并核对差异] [返回编辑]
  不自动覆盖，不自动重试保存或确认

公共 / 状态读取失败
  状态暂无法确认；显示的是此前读取内容
  [重试读取]；依赖最新状态的写操作暂停，已有阅读保留
```

X01 创建/进入：名称表单、真实失败、恢复最近步骤（只持久化导航身份，不存正文）。X02 资料抽屉：用途、读取状态、排除影响与来源定位。X03 需求详情：ID/版本/原文/来源/关系、保存影响。X04 采用 D2。X05–X08、X10–X12 由上述状态与实际门禁接线覆盖。X09：401 显示重新登录，403 显示权限拒绝；本地输入在当前会话可保留，但不承诺刷新恢复。

## 6. 图示纠错逐项落点

| 清单 | 实施落点 |
|---|---|
| C01 | 六步名称使用唯一常量，改正 MFD 等图内错字 |
| C02–C04 | 文件/字数能力来自真实限制；待上传不计已入项目；提示“先提供已知信息，缺失项稍后澄清” |
| C05 | 验证使用统一增量场景，另加不同领域场景，不拼接图中样例 |
| C06–C09 | 回答无默认选择；建议只填草稿；未知分栏；阻塞按服务端；无依据不生成标准值 |
| C10–C11 | 确认草图不触发成文；只调整对照工作台，不实现图中 CRM/批量删除业务 |
| C12–C14 | 文档/日期/检查结果取真实对象，未检查显示未检查；评审与正式确认分开 |
| C15 | 保留完整真实编号与版本，可折行/复制，不重编号 |

## 7. 原实施与验证计划

1. M2a：统一框架、P01–P03、资料/需求/局部帮助抽屉、未保存状态；接入现有 API，先消除双入口。
2. M2b：P04 现状对照与候选；P05 正文和双文档评审；P06 指定版本确认、基线与取包状态。
3. M3：隔离夹具覆盖 UX-01–UX-17 的状态/接口路径及新增最小接口；完整 pytest 与隔离构建。
4. 在唯一实际工作台完成当前构建的浏览器检查：1440/1280/1024、键盘/焦点、正常路径、失败/部分失败/409/过期/输入保留；任何隔离测试实例不能作为另一个长期用户入口。
5. 真实模型复测如需执行，单独记录实际授权、材料和调用；本设计确认不自动启动付费任务。正式业务确认仍留给用户，测试确认只证明机制。

以下追加实际结果，以上保留设计核对时的计划身份；不回写旧试跑结论。


## 8. 实际交付与授权（2026-09-25）

用户要求“完全按我提供的设计资料整改”“UI 严格按照设计稿”，并明确授权隔离浏览器检查及更新 8765。本轮延续现有分支及 PR #2，未重构成文算法、未接入 DSH、未创建长期第二工作台。

- 框架按五图实现绿色项目栏、横向六步导航、任务标题和单一主工作区。永久通用聊天栏已移除；局部帮助绑定需求、问题、草图或文档章节，默认关闭。
- P01 一份核心诉求，页面及保持项可补充；P02 现状、目标、保持、不包含四卡；P03 按真实需求关系组织问题，回答保存与业务采纳独立。
- P04 原页面与真实功能草图对照，窄屏上下排列，不硬编码图中业务；P05 双文档标签、动态目录、正文、右侧核对和审查状态，历史设置折叠。每份核对绑定实际文档产物及内容；旧整体核对记录保持原语义。
- P06 已实现实际范围、交付版本、门禁与未知、复核确认、不可变基线和指定基线取包。正式确认继续使用服务端门禁与幂等键。
- 保存/离开三选项、失败保留输入、旧结果可查看、409 不自动重放、键盘焦点、下载失败可见。范围外未知没有冒充已回答。
- 最小后端新增：统一输入保存、人工候选创建、原子批量问题回答、逐文档核对及局部帮助对象校验；模型/密钥/Origin/CSRF/引用和确认规则仍沿用现有机制。

文件范围：`app/actions.py`、`app/main.py`、`app/product_flow.py`；`web/src/{main,workspace,product-flow,workbench,panels,guided,shell}.tsx`、`api.ts`，新增 `editing.tsx`、`workflow.css`；新增 `tests/test_ui_workflow_contract.py`，调整既有 HTTP 流程夹具和 product_flow_helpers 接入逐文档核对。没有删除旧断言或降低旧判定。

## 9. 实际命令及结果

工作目录均为 `D:\01_AI工程\01_工程项目\pm-req`；测试使用隔离 RA_DATA_DIR，构建先写忽略目录。

| 命令/操作 | 退出码与实际结果 |
|---|---|
| `.venv/Scripts/python.exe -m pytest tests/test_ui_workflow_contract.py tests/test_product_flow.py -q`（早期） | 0；9 项，2 个依赖弃用警告 |
| `.venv/Scripts/python.exe -m pytest -q`（首次完整尝试） | 1；误收集 evidence 中归档源码，133 个 import mismatch 收集错误；保留记录 |
| `.venv/Scripts/python.exe -m pytest tests -q`（限定正式测试目录） | 0；先 189 项；补齐回归后最终 193 项，2 个依赖弃用警告，71.93 秒 |
| `.venv/Scripts/python.exe -m pytest tests/test_ui_workflow_contract.py -q`（最终目标） | 0；8 项契约检查 |
| `npm --prefix web run build -- --outDir ../evidence/ui-workflow-handoff/dist` | 0；TypeScript 检查及 Vite 构建完成，最终 JS `index-qvbXm3-8.js`、CSS `index-DEP836NT.css` |
| `git diff --check` | 0；无空白错误 |
| `scripts/stop.ps1` / `scripts/start.ps1 -NoAuth` | 服务已按原无令牌模式停止/恢复；最终启动退出 0。一次外围 `$LASTEXITCODE` 检查误报退出 1，实际停止成功；核实无监听后继续更新，未结束未知进程 |

8 项新增检查覆盖：统一输入的原文/来源/冲突；候选独立与批量回答原子性；同底稿不同产物的评审身份；帮助对象类型及 hash；文档过期及前序阶段；旧核对兼容；计划与实际对象身份；陈旧回答拒绝且不覆盖。完整测试集合保留 R1–R3、预算、失败证据、Word 章节、密钥绑定与确认机制回归。

## 10. 正常浏览器操作证据

使用当前真实应用与独立合成数据，经用户批准临时运行 8771；模型端为回环受控 HTTP 夹具，没有外部模型请求。浏览器从项目创建开始操作，没有在日常数据库中制造完成记录。

| 路径/状态 | 实际观察 |
|---|---|
| 创建→输入→分析 | 保存后离开可用；初次夹具输出含非法临时问题 ID，被应用拒绝并保留输入；修正夹具后另开任务成功，旧失败仍保留 |
| 核对范围→澄清 | 范围保存/核对、单独采纳条目、批量保存回答；“暂时不知道”仍为未知，建议只填草稿；变更归属后服务端要求重新核对范围 |
| 功能草图 | 应用生成实际 spec 并渲染，核对说明/继续独立，不隐式生成文档 |
| PRD→MRD | 分别生成并核对各自实际产物；缺 MRD 时不能进入交接；切换目录及文档后恢复已选章节阅读位置 |
| 下载错误 | 注入 DOCX HTTP 500，页面显示错误且正文保留；后续正常下载完成，没有示例文本替代 |
| 正式确认机制 | 未审查先阻塞；夹具审查后开放；注入确认 409 后保留对话框且不重放。重新核对后显式确认，仅证明机制，不是业务负责人批准 |
| 基线交接 | 合成场景真实服务端生成不可变基线及其交接包，出现对应下载入口 |
| 离开/失败 | 项目切换弹出保留/保存/放弃；保存注入 403 后输入保留；明确放弃才导航 |
| 辅助与键盘 | 章节帮助绑定实际章节；历史/帮助对话框 Tab 焦点限制、Escape 关闭后返回触发按钮 |
| 尺寸 | 隔离 P02 的 1440/1280/1024 均核对实际 viewport、截图与横向溢出；最终日常 P05 三宽度及 P04 1024 复核无页面横向溢出；检查时浏览器控制台错误为空 |

另一个电价命名的合成项目仅检查输入保护，没有把它算作第二条完整业务链路。首次夹具失败与后续任务分开，未回填失败响应。

本机证据目录 `evidence/ui-workflow-handoff/`（忽略，不提交）：
- `final-p01.png` 至 `final-p06.png`：隔离应用六步页面；`p06-conflict.png`：冲突状态。
- `verified-p02-1440.png` / `1280.png` / `1024.png`：实际尺寸检查。
- `daily-p05-1440.png` / `1280.png` / `1024.png`、`daily-p04-1024.png`：最后安装构建的只读截图。
- `pytest-delivery.txt`：193 项最终工程回归。
- `browser-data-02/`、`browser-http.jsonl`：合成执行记录，保持本机，不进入公开提交。

## 11. 唯一入口与剩余边界

8765 更新前确认无活动任务，使用 SQLite 一致性备份及旧 dist 备份。更新前后 8 个已有项目的 revision 与原始 payload SHA-256 全部一致。最终 backend runtime_id `118f2f1560bb00ef`；页面实际加载上述最终资源。隔离 8771 和本轮回环夹具均已关闭，日常入口仍为 `http://127.0.0.1:8765/`。没有改动既有 `.env.example` 删除、`原型/` 或用户私有记录。

NOT_RUN / 不构成的结论：
- 本轮未进行外部真实模型复测、真人业务首审或正式发布验收；历史真实模型失败保持原样。
- 本轮浏览器未重新执行中文 IME 组合输入、401 重新登录、上传失败后选中文件保留、取消任务竞态及候选采纳/拒绝全部路径；不能拿工程测试代替这些浏览器结果。
- 本轮没有重新渲染 Word 做页面视觉验收；下载和既有导出回归不替代 Word 视觉验收。
- 五张输入图是概念图；第六步依据交接清单补齐，图中示例数字/业务没有当作产品数据。当前交付是已接线、已执行离线与浏览器核对的工作台，仍需用户体验验收。
