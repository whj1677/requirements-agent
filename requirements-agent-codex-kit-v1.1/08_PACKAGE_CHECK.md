# 交付包自身校验记录

版本：1.1｜日期：2026-09-23。范围仅为本开发包，不是需求 Agent 应用测试。

## 已执行

1. 8 个配置／Schema／样例 JSON 可解析（此数不含随后生成的 PACKAGE_MANIFEST.json）。

2. 两份 JSON Schema 自身合法；10 份阶段协议样例及一份 wireframe 样例通过对应结构校验。PRD／MRD 仅为局部协议样例，不是完整文档。

3. 提示词 registry 中的阶段、Schema、章节 profile 文件引用均存在；未将 MRD 增设为独立 Agent。

4. 旧 EV-01～EV-08 的评测内容逐项保持不变，新增 EV-09；模型配置和 wireframe Schema 原件哈希未改。

5. 两份参考 DOCX 与用户上传原件 SHA-256 一致；PRD 33 项和 MRD 19 项参考目录记录的 ID、父关系与来源哈希有效。

6. 局部 PRD／MRD 样例的 document_type、profile、章节层级、实际章节引用一致；未据此宣称全部内容维度已覆盖。

7. 11 个结构反例被 Schema 拒绝：外壳伪造确认、阶段与结果不匹配、问题伪造审批、规范性条款改写、线框任意脚本字段、一次结果混合两文档类型、非法标题层级、不适用缺少理由、合并没有目标章节、待确认缺少说明、新接口误收1.0外壳。

8. 另外 3 个局部样例引用反例被包内检查拒绝：PRD错用MRD profile、映射悬空输出章节、标题父节点自指。这不是应用运行时门禁测试。

9. AT-01～AT-43 编号唯一，旧编号保留；9 组真实模型评测仍为待执行设计，计划为每组3次共27次。

10. Markdown 围栏成对、内部工具引用标记未混入、人工目录不超过3层；提示词合订版与分文件一致；过时的“未提供模板”等描述已替换。

## 本次来源核对

已按两份原件的已提取正文及可见页面图表核对章节内容：PRD 第一至第五章、逐功能与非功能维度；MRD 第1至第7章、功能详述及裁剪说明。这里的“核对”仅说明参考规范有材料依据，不证明尚未实现的导出器正确。

## 未执行

没有实现或修改用户本地 Agent 代码；没有真实 API 调用；没有运行产品 E2E 或业务用例；没有新生成业务 PRD／MRD Word 并做其渲染验收；没有真人产品首审。

包内两份 Word 是原始参考附件，按原字节保存，未宣称修复其版式。模型配置与技术来源沿用 v1.0，本次未重新检索或验证模型能力。

因此，以上结果只说明开发包、参考映射与样例的基本一致性，不证明模型成文质量、软件安全、Word 输出效果或产品完成。
