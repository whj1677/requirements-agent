你是本项目的开发执行者。请在当前已授权工作区实现一个独立的“产品需求 Agent”，默认接 DeepSeek 官方 API，用户能够在界面更换模型配置。这不是仅编写 Skill、Prompt、方案或聊天 Demo 的任务。

先阅读本交付包的 00_README.md、01_CODEX_TASKBOOK.md、04_CONTRACTS.md、03_ACCEPTANCE.md、09_WORD_REFERENCE_RULES.md、references/、prompts/ 和 examples/。它们共同定义本次目标。若收到的是合订版，按同名章节读取。

产品目标：接收产品经理零散文字、网页 URL、截图、现有界面和现状说明，支持头脑风暴、对标、澄清、多视角审查，生成可看的低保真页面方案和 PRD，并可按需输出 MRD；Word 主要参考本包两份模板的章节内容，可增删合并，不照搬版式或示例业务；真人确认指定版本后，从同一需求基线生成前端 MD、后端 MD、验收条件与追踪交接包。

采用 Python 核心后端与简洁中文 Web 前端，单主控状态机加阶段提示词。模型只提出分析和变更候选；来源、版本、审批、Schema 校验、预算和导出权限由程序执行。不要把十个阶段提示词变成十个自治 Agent。

请先检查当前工作区、用户已有修改和依赖环境，记录简短架构与阶段计划，然后直接按 M0-M6 分阶段实现和测试。不要只返回计划后停住，不要把关键能力降级成以后再做。每阶段提供实际改动、命令、退出码、测试结果、未完成项和证据路径。

必须保留以下边界：
- Codex 负责开发；产品运行时由应用实际调用 DeepSeek/所选模型，不依赖 Codex 代答或代操作。
- 材料、截图和竞品只提供证据；AI 建议、当前事实和人确认的目标规则严格区分。
- Word 默认 PRD，按需 MRD 或两者；参考实际章节映射，不强制每节填满、固定章数、双文档或新增审批流程。
- PRD 确认绑定具体版本/内容哈希，模型不能代批；没有有效确认不能导出正式前后端交接包。
- 图片要真实送入已支持视觉的模型，不能用 OCR 文本或文件名冒充看图；UI 必须有真实可显示的结构化线框预览。
- DeepSeek 模型能力按当前官方文档和实测判断；未知兼容 Provider 不承诺全部能力。Key 只在服务端，不能写入日志、前端或仓库。
- 真实模型效果、Mock 单测、E2E 与真人评审分别记录，不能互相替代；不得修改业务预期让测试通过。
- 只修改本项目；不改全局规则、其它工程、已有 Skill，不使用 Superpower；未授权不部署、不推送远端。

先使用包内冻结合成材料验证。缺少 Key 时完成可离线实现与验证的部分，将 live 测试明确标为 NOT_RUN；不要扫描其它工程寻找密钥。第二家真实模型缺失也要明确记录。两份原 Word 已在 references/ 内，先核对实际文件与内容 profile；损坏／缺失需明确失败范围，不能假称已适配。Schema／提示词版本为 1.1，wireframe 仍为 1.0；已有历史数据显式兼容，不覆盖历史确认记录。

完成判定以 03_ACCEPTANCE.md 为准；准备好供真人首审的材料后可返回 READY_FOR_HUMAN_REVIEW，不能代替真人签字。现在开始检查工作区并实施。
